/** @noSelfInFile **/
import { Game } from "../game";


export class GeneModule {   
    

    constructor(game: Game) {
        // print("Adding research to player 1 "+GetPlayerName(Player(1)));
        // AddPlayerTechResearched(Player(0), FourCC("R000"), 1);
    }
}